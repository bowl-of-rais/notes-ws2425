$U \in \mathbb{C}^{n \times n}$ with $$U^{\dagger}U = I$$
aka [[conjugate transpose]] is the inverse

- change of basis preserving length and angles between vectors
- $U$ unitary iff $U^{\dagger}$ unitary